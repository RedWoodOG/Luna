[package]
name = "luna-tether"
version.workspace = true
edition.workspace = true
description = "Directional typed tethers for Luna topology."

[lib]
path = "src/lib.rs"

[dependencies]
luna-core = { path = "../luna-core" }
luna-ledger = { path = "../luna-ledger" }
luna-node = { path = "../luna-node" }
serde.workspace = true
